extends layout

block content
  h1= title
  - let watch = results
  div(class="container-fluid")
  div(class="row")
   each w,index in watch
    div(class="col-sm-2" onclick =`buy(${index})`) #{w.watch_brand}, #{w.watch_cost}, #{w.watch_color}
   script.
    function buy(i)
    {
      alert(`You purchased Item ${i+1}`)
    } 
